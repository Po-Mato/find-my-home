# NCP 콘솔에서 웹 서비스 URL 등록하기 (단계별)

**목표**: 네이버 클라우드 플랫폼(NCP) 콘솔에서 Maps Application에 `http://localhost:8080` 도메인 추가

---

## 📸 Step-by-Step 가이드

### Step 1: NCP 콘솔 접속

**URL**:
```
https://console.ncloud.com
```

**로그인**:
- 네이버 클라우드 계정으로 로그인

---

### Step 2: Maps 서비스 찾기

**메뉴 경로**:
```
좌측 메뉴
└─ Application Services
   └─ Maps
```

**또는 검색**:
- 상단 검색창에 "Maps" 검색 → 클릭

---

### Step 3: Application 선택

**현재 상황**:
- Application 목록이 표시됨
- Application 이름: `ual79sk3ij` (또는 등록한 이름)

**액션**:
- Application 이름 클릭 (상세 페이지 진입)

---

### Step 4: Application 정보 확인

**표시되는 정보**:
```
┌─ Application 정보
│  ├─ Client ID: ual79sk3ij
│  ├─ API Gateway Key: ...
│  ├─ Status: Active
│  └─ 웹 서비스 URL
│     └─ [현재 등록된 URL 목록]
│
└─ 버튼
   ├─ [수정]  ← 클릭!
   └─ [삭제]
```

---

### Step 5: [수정] 클릭

**상세 편집 페이지 진입**:
- Application 이름 (변경 가능)
- 서비스 선택 (Maps ✅ 확인)
- **웹 서비스 URL** ← 이곳이 중요!

---

### Step 6: 웹 서비스 URL 섹션

**현재 상황** (예상):
```
웹 서비스 URL
├─ https://example.com      ← 기존에 등록된 도메인
└─ [+ URL 추가] 버튼
```

**또는 비어있음**:
```
웹 서비스 URL
├─ (등록된 URL 없음)
└─ [+ URL 추가] 버튼
```

**필요한 URL 목록**:
```
개발 환경:
□ http://localhost:8080
□ http://localhost:8080/
□ http://127.0.0.1:8080

향후 프로덕션:
□ https://yourdomain.com  (Vercel 배포 후)
```

---

### Step 7: URL 추가하기

**방법**:
1. **[+ URL 추가]** 버튼 클릭
2. 텍스트 입력 필드 나타남
3. **첫 번째 URL** 입력: `http://localhost:8080`
4. 엔터 또는 [추가] 버튼
5. **두 번째 URL** 입력: `http://localhost:8080/`
6. 엔터 또는 [추가] 버튼
7. **세 번째 URL** 입력: `http://127.0.0.1:8080`
8. 엔터 또는 [추가] 버튼

**최종 상태**:
```
웹 서비스 URL
├─ http://localhost:8080
├─ http://localhost:8080/
├─ http://127.0.0.1:8080
└─ https://yourdomain.com  (기존)
```

---

### Step 8: [저장] 버튼 클릭

**페이지 하단**:
```
[취소] [저장]
        ↑ 클릭!
```

**저장 확인**:
```
✅ Application이 정상적으로 수정되었습니다.
```

---

## ✅ 확인 체크리스트

```
저장 후:
☐ 콘솔 메시지 "정상적으로 수정되었습니다" 확인
☐ 웹 서비스 URL 목록에 3개 URL 모두 표시됨
☐ NCP 콘솔 페이지 닫기 (선택사항)
```

---

## 🔄 로컬 개발 환경에서 확인

### 개발 서버 재시작
```bash
# 1. 기존 서버 종료
pkill -f "pnpm dev"

# 2. .next 캐시 삭제 (중요!)
rm -rf /Users/sjlee/develop/find-my-home/.next

# 3. 서버 재시작
cd /Users/sjlee/develop/find-my-home
pnpm dev
```

### 브라우저에서 확인
```
1. http://localhost:8080 열기
2. Cmd + Shift + R (강제 새로고침)
3. Chrome DevTools > Console 탭 열기 (Cmd + Option + J)
4. 아래 메시지가 나타나는지 확인:
   ✅ [NaverMap] 초기화 시작
   🔔 [NaverMap] initNaverMap 콜백 호출됨
   ✅ [NaverMap] 지도 인스턴스 생성 완료
```

**인증 오류가 사라져야 합니다!** ✅

---

## 🆘 만약 못 찾겠다면?

### 1. NCP 대시보드 우측 상단
```
프로필 아이콘 > [마이페이지]
↓
좌측 메뉴: Application Services
```

### 2. 직접 URL로 접속
```
https://console.ncloud.com/codeplatform/products/ncloud/maps
```

### 3. 네이버 클라우드 고객센터
```
https://www.ncloud.com/support/question/general
```

---

## 📋 참고: 전체 Application 정보 확인

**NCP 콘솔**:
```
Application 상세 정보
├─ Client ID: ual79sk3ij ← 맞음 ✅
├─ Client Secret: (표시 안 함)
├─ API Gateway Key: (표시됨)
├─ Service: Maps ← 활성화 ✅
├─ 웹 서비스 URL: ← 이곳에 URL 추가! 🎯
│  ├─ http://localhost:8080
│  ├─ http://localhost:8080/
│  └─ http://127.0.0.1:8080
└─ Status: Active
```

---

**완료!** ✅

NCP 콘솔에서 웹 서비스 URL을 추가하면 인증 오류가 해결됩니다.
